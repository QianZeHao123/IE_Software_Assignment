# 目标函数

# 约束方程
'''
|x_j - x_i| + (v_i + v_j) / 2 <= L
|y_j - y_i| + (h_i + h_j) / 2 <= W
|x_j - x_i| >= (v_i + v_j) / 2 + delta_h_ij
|y_j - y_i| >= (h_i + h_j) / 2 + delta_v_ij
'''

# import artiBeeColony_algorithm as abc
# import numpy as np
# import matplotlib.pyplot as plt
